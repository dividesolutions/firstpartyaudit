{
  "name": "tracking-audit-full-v2",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/server.ts",
    "start": "node --enable-source-maps dist/server.js",
    "build": "tsc -p tsconfig.json",
    "postinstall": "npx playwright install --with-deps || true"
  },
  "dependencies": {
    "fastify": "^4.28.1",
    "playwright": "^1.46.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "tsx": "^4.16.0",
    "typescript": "^5.6.2"
  }
}
